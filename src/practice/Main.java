package practice;

public class Main {
    public static void main(String[] args) {
        NotificationManager manager = new NotificationManager();
        manager.execute("hello people");
        manager.execute("hello synesis it");
    }
}
